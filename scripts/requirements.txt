# Documentation Pipeline Requirements
# Install: python -m venv .venv && source .venv/bin/activate && pip install -r scripts/requirements.txt

# Core dependencies
pyyaml>=6.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
html2text>=2024.2

# Template engine (for doc generation)
jinja2>=3.1.5

# Development tools
ruff>=0.4
pytest>=8.0

# Branding & Assets (for generate-placeholder-assets skill)
Pillow>=10.0
cairosvg>=2.7

# Screenshot Capture (post-v1, optional for now)
# playwright>=1.40  # Uncomment when implementing capture-screenshots skill

# Optional: for faster YAML parsing
# ruamel.yaml>=0.18

# Schema Validation
jsonschema>=4.0
