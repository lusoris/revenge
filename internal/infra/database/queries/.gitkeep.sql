-- Placeholder for sqlc queries
-- This file ensures the queries directory exists for sqlc
-- Add actual queries when implementing repositories

-- Example query structure:
-- -- name: GetUser :one
-- SELECT * FROM users WHERE id = $1 LIMIT 1;
