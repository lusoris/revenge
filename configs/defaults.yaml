# Default Configuration
server:
  host: 0.0.0.0
  port: 8096

database:
  type: sqlite
  path: ./data/jellyfin.db
  host: localhost
  port: 5432
  user: jellyfin
  name: jellyfin

cache:
  type: memory
  addr: localhost:6379
  db: 0

log:
  level: info
  format: console
