# Jellyfin Go Configuration

server:
  host: 0.0.0.0
  port: 8096

database:
  type: sqlite  # sqlite or postgres
  path: ./data/jellyfin.db
  
  # PostgreSQL settings (when type is postgres)
  # host: localhost
  # port: 5432
  # user: jellyfin
  # password: changeme
  # name: jellyfin

cache:
  type: memory  # memory or redis
  
  # Redis settings (when type is redis)
  # addr: localhost:6379
  # password: ""
  # db: 0

log:
  level: info  # debug, info, warn, error
